	<% form_for @item, :url => url do |f| %>
		<%= f.error_messages %>
		<%= f.label :description %><%= f.text_field :description, :class=>'input' %>
		<%= f.label :value %><%= f.text_field :value, :class=>'input',:value=> value %>
		<%= f.submit button_text %>
	<% end %>